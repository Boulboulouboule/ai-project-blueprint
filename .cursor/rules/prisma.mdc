---
description: Prisma schema conventions and migration workflow
globs: ["packages/db/**", "**/schema.prisma"]
---

# Prisma Rules

## Schema Conventions
- Models use PascalCase: `model User { ... }`
- Fields use camelCase: `firstName`, `createdAt`
- Always include `id`, `createdAt`, `updatedAt` on every model
- Use `@default(cuid())` for IDs (shorter than UUID, URL-safe, sortable)
- Add `@@map("table_name")` for snake_case table names in PostgreSQL
- Add `@map("column_name")` for snake_case column names

```prisma
model UserProfile {
  id        String   @id @default(cuid())
  email     String   @unique
  firstName String   @map("first_name")
  lastName  String   @map("last_name")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  @@map("user_profiles")
}
```

## Client Pattern
- Export a singleton PrismaClient from `packages/db`
- Use global store pattern to prevent connection exhaustion in dev

```typescript
import { PrismaClient } from '@prisma/client';

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient | undefined };

export const db = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== 'production') globalForPrisma.prisma = db;
```

## Migration Workflow
1. Edit `schema.prisma`
2. Run `pnpm db:migrate` (alias for `prisma migrate dev --name <description>`)
3. Commit migration files alongside schema changes
4. CI/production: `prisma migrate deploy` (applies pending migrations)

## Queries
- Use `select` to pick only needed fields (avoid over-fetching)
- Use `include` sparingly — prefer separate queries to avoid N+1
- Enable query logging in development: `new PrismaClient({ log: ['query'] })`
- Use transactions for multi-step operations: `db.$transaction([...])`

## Seeding
- Seed script in `packages/db/src/seed.ts`
- Use `upsert` for idempotent seeding
- Run via `pnpm db:seed`
